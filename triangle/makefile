Triangle: triangles.cpp main.cpp object.cpp
	g++ -Wall -std=c++11 -O3 -o Triangle triangles.cpp camera.cpp object.cpp main.cpp -lSDL -lglut -lGLU -lGL

.PHONY: clean
clean:
	rm -f $(obj) ./Triangle .*.swp
